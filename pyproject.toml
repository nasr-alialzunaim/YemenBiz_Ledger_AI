[project]
name = "yemenbiz-ledger-ai"
version = "0.1.0"
description = "AI-powered Arabic business ledger for Yemeni small shops."
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
